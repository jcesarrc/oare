package co.gov.mme.oare.ws.helpers;



public class GlpHelper {

    public static String calcularValorCilindroConSubsidio(String subsidio){

        /*
        double valorTotal = valorCilindro - Convert.ToDouble(dr.ItemArray[0].ToString());
        long subsidio = Convert.ToInt64(dr.ItemArray[0].ToString());
        response = subsidio.ToString();
        */

        return subsidio;
    }

}